## Контекст

NFT Аренда / Покупка полностью убраны. Premium остаётся на кнопках (3/6/9 мес), не слайдер. План — пошаговая доработка под ТЗ.

---

## Блок 1. Premium 3/6/9 мес + UX кнопки

- Миграция данных: у продуктов с `product_type='premium_term'` поле `term_options` обновить на `[{months:3,price:…},{months:6,price:…},{months:9,price:…}]`
- `PremiumTermCard`: оставить кнопки выбора срока; кнопка «Добавить в корзину» **дизейблится до явного выбора** (сейчас сразу выбран 0-й)
- Цена пересчитывается при смене кнопки

## Блок 2. Звёзды — непрерывный слайдер 0..10000

- `StarsCard`: заменить слайдер по пресетам на непрерывный диапазон **1..10000** (шаг 1, дефолт `min_qty` из продукта)
- Кнопка «Добавить» дизейблится при qty<1
- Цена считается динамически (`qty × price`)
- Поле максимума — из `products.max_qty` (fallback 10000)

## Блок 3. Статусы заказа

- Привести `orders.status` к набору ТЗ §8.3: `new`, `pending_payment`, `paid`, `processing`, `done`, `cancelled`, `error`
- Маппинг текущих значений (`pending` → `pending_payment`, `unpaid` → `pending_payment`, `paid`→`paid` и т.д.) в `src/types/database.ts` + UI-лейблы
- Обновить отображение в `OrderDetailSheet`, истории профиля, `OrderStatus.tsx`
- `username` пользователя добавить в выдачу заказа (`get-my-data`)

## Блок 4. FLUX — примеры работ

- В карточке товара (`ProductDetails.tsx`) добавить блок «Примеры работ» под кнопкой цены для проекта FLUX
- Источник данных — `products.gallery` (массив объектов `{url, link}`) либо `external_link`
- Превью кликабельны, открывают внешний ресурс
- Каталог FLUX — проверить список без фото

## Блок 5. VIETO — категории

- Проверить навигацию `project=vieto → category → product list → product card`
- Список товаров без фото (только название)
- Карточка стандартная

## Блок 6. CURSOR — каталог-список

- Список товаров: Premium, НФТ, Звёзды — без фото, только названия
- Убедиться, что блок NFT Аренда/Покупка нигде не отображается

## Блок 7. Корзина — финальная зачистка

- Удалить любые остатки UI выбора срока аренды (после прошлой очистки проверить)
- Перенос длинных названий, отсутствие баннеров
- Тост при добавлении — проверить везде

## Блок 8. Серверные пересчёты + ошибки

- `create-invoice` / `pay-with-balance`: пересчитывать сумму и применение промокода на сервере, не доверять клиентскому `amount`
- Авто-удаление из корзины недоступного товара с тостом
- Предупреждение при изменении цены до оплаты

## Блок 9. `/rep` и админ-команды

- Аудит `telegram-bot`: `/rep #номер` — только админы (`ADMIN_TELEGRAM_IDS`), лог в `admin_log`, ЛС пользователю, формат с номером
- Корректные ошибки: «заказ не найден», «ЛС закрыты»
- Проверить, что админка покрывает: shop_name, marquee, баннеры, продукты/цены, отзывы, FAQ url, ссылки примеров FLUX, вкл/выкл товара

## Блок 10. Пустые состояния и финал

- Заглушки: пустой раздел, пустая категория, пустая корзина (уже есть — проверить)
- Тосты ошибок загрузки
- Удалить мёртвый код, ассеты, edge-функции, связанные с NFT арендой/покупкой/Portals (если Portals больше не нужен — также убрать)

---

## Технические детали

- Миграции: `term_options` для Premium
- Файлы: `SpecialProductCards.tsx` (Premium, Stars), `ProductDetails.tsx` (FLUX gallery), `Cart.tsx`, `OrderDetailSheet.tsx`, `OrderStatus.tsx`, `get-my-data`, `create-invoice`, `pay-with-balance`, `telegram-bot`
- Типы: `src/types/database.ts` — enum статусов + хелпер `orderStatusLabel`

## Порядок исполнения

Идём строго блок за блоком (1 → 10). После каждого блока показываю результат и переключаюсь к следующему.